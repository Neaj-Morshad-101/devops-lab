provisioning test for coordinator change with (ready -> restart-> ready): 
cu19 (std ok, ag ok), 
cu22 (std ok, ag ok)
cu0 (std ok, ag ok)

provitioning:
normal (worked)
with secret + apply: ok


reconfigure behavior: testing done all OK 
This is the finalized behaviour (Giving all combinations of remove, apply, and configSecret is allowed):


If RemoveCustomConfig is false:
0. nothing 
1. Only applyConfig is given: keep the previous custom config if it exists. Merge the applyConfig with the previous applyConfig if it exists. Otherwise, just use the given applyConfig, keep these configs in the DB CR, also in -uid[6] secret (for mounting to pods).
3. Only configSecret is given: Use the newly given configSecret as the custom config secret. Keep the previous applyConfigs as well.
4. config + apply: Allowed. Use both. For merging, give priority to applyconfig.



if RemoveCustomConfig is true:
4. No applyConfig and ConfigSecret are given: Remove the previous custom configurations (applyConfig and configSecret)
5. Only applyConfig is given: Remove previously used custom configurations, and use the configuration given in the applyConfig.
6. configSecret is given: Remove previously used custom configurations, and use the configuration given in the configSecret.
7. apply + config secret is given: merge 



New mssql version test: all ok
standalone 2022-cu22 (ok)  2025-cu0 (ok)
ag cu22 (ok) cu0 (ok)




testing started: (done)


➤ kubectl view-secret -n demo ms-custom-config
Viewing only available key: mssql.conf
[network]
tlsprotocols = 1.2

[language]
lcid = 1033

[memory]
memorylimitmb = 2304

standalone.yaml
  configuration:
    secretName: ms-custom-config
    applyConfig:
      mssql.conf: |-
        [memory]
        memorylimitmb = 2596
        [network]
        tcpport = 1433


provision: 
➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033
[memory]
memorylimitmb = 2596
[network]
tcpport = 1433
tlsprotocols = 1.2

ok 









Provision: OK 

If RemoveCustomConfig is false:
0. OK (fixed) nothing 

➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033
[memory]
memorylimitmb = 2596
[network]
tcpport = 1433
tlsprotocols = 1.2



1. OK Only applyConfig is given: keep the previous custom config if it exists. Merge the applyConfig with the previous applyConfig if it exists. Otherwise, just use the given applyConfig, keep these configs in the DB CR, also in -uid[6] secret (for mounting to pods).

➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033
[memory]
memorylimitmb = 3096
[network]
tcpport = 1433
tlsprotocols = 1.2


  configuration:
    applyConfig:
      mssql.conf: |
        [memory]
        memorylimitmb = 3096
        [network]
        tcpport = 1433
    secretName: ms-custom-config


2. OK Only configSecret is given: Use the newly given configSecret as the custom config secret. Keep the previous applyConfigs as well.

➤ kubectl view-secret -n demo ms-custom-config2
Viewing only available key: mssql.conf
[memory]
memorylimitmb = 2056

---


➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033
[memory]
memorylimitmb = 3096
[network]
tcpport = 1433


  configuration:
    applyConfig:
      mssql.conf: |
        [memory]
        memorylimitmb = 3096
        [network]
        tcpport = 1433
    secretName: ms-custom-config2


3.OK config + apply: Allowed. Use both. For merging, give priority to applyconfig.

➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033
[memory]
memorylimitmb = 3096
[network]
tcpport = 1433



  configuration:
    applyConfig:
      mssql.conf: |
        [memory]
        memorylimitmb = 3096
        [network]
        tcpport = 1433
    secretName: ms-custom-config2

-- 

➤ kubectl view-secret -n demo ms-custom-config3
Viewing only available key: mssql.conf
[memory]
memorylimitmb = 3056

[network]
tcpport = 1433
tlsprotocols = 1.2


-- after apply---
➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033
[memory]
memorylimitmb = 5096
[network]
tcpport = 1433
tlsprotocols = 1.2

  configuration:
    applyConfig:
      mssql.conf: |
        [memory]
        memorylimitmb = 5096
        [network]
        tcpport = 1433
    secretName: ms-custom-config3






if RemoveCustomConfig is true:
4. OK No applyConfig and ConfigSecret are given: Remove the previous custom configurations (applyConfig and configSecret)

now: 
configuration: nil 

➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033



5. Only applyConfig is given: Remove previously used custom configurations, and use the configuration given in the applyConfig.
6. configSecret is given: Remove previously used custom configurations, and use the configuration given in the configSecret.
7. OK apply + config secret is given: merge 


➤ kubectl view-secret -n demo ms-custom-config3
Viewing only available key: mssql.conf
[memory]
memorylimitmb = 3056

[network]
tcpport = 1433
tlsprotocols = 1.2


➤ kubectl view-secret -n demo standalone-6ca9a0
Viewing only available key: mssql.conf
[language]
lcid = 1033
[memory]
memorylimitmb = 7096
[network]
tcpport = 1433
tlsprotocols = 1.2


  configuration:
    applyConfig:
      mssql.conf: |-
        [memory]
        memorylimitmb = 7096
    secretName: ms-custom-config3



