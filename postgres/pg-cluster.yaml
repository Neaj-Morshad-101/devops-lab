apiVersion: kubedb.com/v1
kind: Postgres
metadata:
  name: pg-cluster
  namespace: demo
spec:
  version: "17.5"
  replicas: 3
  standbyMode: Hot
  storageType: Durable
  storage:
    # storageClassName: "linode-block-storage" #"standard"
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
  # monitor:
  #   agent: prometheus.io/operator
  #   prometheus:
  #     serviceMonitor:
  #       labels:
  #         release: prometheus
  #       interval: 10s
  podTemplate:
    spec:
      containers:
      - name: postgres
        resources:
          requests:
            cpu: "1.5"
            memory: "2Gi"
          limits:
            memory: "4Gi"