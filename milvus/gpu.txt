- needs nodes with gpu: Your node must have an NVIDIA GPU + NVIDIA drivers installed
On the node (Ubuntu, Debian, etc.): `nvidia-smi` Must work.


- use gpu-image

- set gpu config

✅ You must have an NVIDIA GPU plugin installed on the cluster (NVIDIA Device Plugin or GPU Operator).
2️⃣ Install the NVIDIA Device Plugin (DaemonSet): 
This is what enables:
nvidia.com/gpu
apply:
kubectl apply -f https://raw.githubusercontent.com/NVIDIA/k8s-device-plugin/v0.15.0/nvidia-device-plugin.yml
This creates: nvidia-device-plugin-daemonset

On every GPU node, it will expose:
status:
  capacity:
    nvidia.com/gpu: "1"

```
resources:
  limits:
    nvidia.com/gpu: 1   # GPU REQUEST
```


